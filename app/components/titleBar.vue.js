var titleBar={
    name:"title-bar",
    props:['height'],
    template:
    `<div :style="{height: this.height}" class="title-header">
        <h1 class="title-font">Women in Los Alamos Computing</h1>
    </div>`
}